
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home</title>
</head>
<body>
  <h1>Welcome to the Rental System</h1>
  <nav>
  {{#if status}}
    <a href="/logout">Logout</a> |
  {{else}}
    <a href="/login">Login</a> |
  {{/if}}

  <a href="/signup">Sign Up</a> |
  <a href="/addProperty">Add New Property</a>
</nav>


  <!-- Check if properties exist -->
  {{#if properties.length}}
    <h2>All Properties</h2>
    <ul>
      {{#each properties}}
        <li>
          <h3>{{this.title}}</h3>
          
          <!-- Display property image if it exists -->
          {{#if this.propertyPictures}}
            <img src="{{this.propertyPictures}}" alt="{{this.title}}" style="width: 300px; height: auto;" />
            <h1>{{this.propertyPictures}}</h1>
          {{else}}
            <p><em>No image available</em></p>
          {{/if}}
          
          <p><strong>Price:</strong> Rs. {{this.price}}</p>
          <p><strong>Address:</strong> {{this.address}}</p>
          <p><strong>Description:</strong> {{this.description}}</p>
          <a href="/property/details/{{this._id}}">View Details</a> |
          <a href="/property/edit/{{this._id}}">Edit</a>|
          <a href="/property/delete/{{this._id}}">  delete </a>
        </li>
        <hr>
      {{/each}}
    </ul>
  {{else}}
    <p>No properties available at the moment.</p>
  {{/if}}

</body>
</html>
