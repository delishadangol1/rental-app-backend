<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
</head>
<body>
    <h1>Edit Profile</h1>

    <form action="/profile/edit" method="POST">
        <!-- Hidden field to hold the user ID -->
        <input type="hidden" name="userId" value="{{user._id}}">

        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="{{user.name}}"><br><br>

        <label for="age">Age:</label>
        <input type="number" id="age" name="age" value="{{user.age}}"><br><br>

        <label for="sex">Sex:</label>
        <select id="sex" name="sex">
            <option value="m" {{#if user.sex}}{{#if (user.sex '===' 'm')}}selected{{/if}}{{/if}}>Male</option>
            <option value="f" {{#if user.sex}}{{#if (user.sex '===' 'f')}}selected{{/if}}{{/if}}>Female</option>
        </select><br><br>

        <button type="submit">Update Profile</button>
    </form>
</body>
</html>
